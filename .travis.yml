language: ruby
before_install:
  - gem install bundler
  - appraisal install
script:
  - bundle exec appraisal rspec

gemfiles:
  - faraday_0.15.gemfile
  # - faraday_1.1.gemfile
  # - rails_4.2.gemfile
  # - rails_5.2.gemfile
  - rails_6.0.gemfile
rvm:
  - 2.6
matrix:
  fast_finish: true
